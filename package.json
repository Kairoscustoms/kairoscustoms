{
  "name": "clothing-website",
  "version": "1.0.0",
  "engines": {
    "node": ">=14.x"
  },
  "scripts": {
    "start": "node server/index.js",
    "postinstall": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.21.2",
    "cors": "^2.8.5",
    "nodemailer": "^6.9.1",
    "multer": "1.4.5-lts.1",
    "dotenv": "^16.0.0"
  }
}

